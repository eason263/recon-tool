# Runtime dependencies for recon-tool.
# Install: pip install -r requirements.txt
# (Versions pinned to the tested environment; pyproject.toml holds the loose ranges.)
click==8.4.1
Flask==3.1.3
Jinja2==3.1.6
openpyxl==3.1.5
python-docx==1.2.0
rich==15.0.0

# Test dependency (safe to keep; remove if you want runtime-only)
pytest==9.0.3
